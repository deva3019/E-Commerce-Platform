# set identity (global) — replace with your name/email
git config --global user.name "deva3019"
git config --global user.email "vinothkumaran1930@gmail.com"

# OR set identity only for this repo:
# git config user.name "deva3019"
# git config user.email "vinothkumaran1930@gmail.com"

# verify
git config --list

# add files and commit
git add README.md
git commit -m "first commit"

# ensure branch is named main
git branch -M main

# check remote (if not set, add it)
git remote -v
# if no origin:
# git remote add origin https://github.com/deva3019/E-Commerce-Platform.git

# push to GitHub
git push -u origin main